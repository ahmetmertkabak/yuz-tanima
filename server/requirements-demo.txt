# Lightweight dependency set for local SQLite demo
# (no Postgres / Redis / face_recognition — webcam face capture still works
# because the backend lazy-loads face_recognition only on enrollment).

# ---- Core ----
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-SocketIO==5.3.6
Flask-Limiter==3.8.0

# ---- DB ----
SQLAlchemy==2.0.32
alembic==1.13.2

# ---- Auth / security ----
bcrypt==4.2.0
pyotp==2.9.0
cryptography==43.0.1
itsdangerous==2.2.0

# ---- Validation / forms ----
WTForms==3.1.2
email-validator==2.2.0

# ---- Utilities ----
python-dotenv==1.0.1
Pillow==10.4.0
openpyxl==3.1.5
requests==2.32.3
click==8.1.7
numpy==1.26.4

# ---- Simple Socket.IO dev mode ----
simple-websocket==1.1.0