[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -ra
    --strict-markers
    --tb=short
markers =
    unit: fast, no external dependencies
    integration: needs database / redis
    slow: long-running tests